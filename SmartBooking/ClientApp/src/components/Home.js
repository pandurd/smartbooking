import React from 'react';
import { Box } from 'grommet';
import MovieList from './MovieList';

const Home = (props) => {
  return (
    <Box>
      <MovieList />
    </Box>
  );
};

export default Home;
